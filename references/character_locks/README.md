# Character Locks

Put one reference image (or Higgsfield-trained reference ID, once confirmed
how that workflow actually works — see scripts/higgsfield_client.py) per
recurring deity/character here, named `<deity>_v1.png` e.g. `krishna_v1.png`,
`vishnu_v1.png`, `shiva_v1.png`.

Chitrkar (see .claude/skills/chitrkar/SKILL.md) requires this path as
input for any shot featuring that character — never generated "from
scratch" per shot. This is empty right now; populate it before running
the pipeline on any premise involving a recurring character.
