# Brand assets

Place the approved transparent watermark at `watermark.png`.

The production preflight intentionally fails until that exact file exists.
Do not substitute a generated or temporary logo during an unattended run.
